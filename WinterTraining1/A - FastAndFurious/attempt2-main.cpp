#include <iostream>
#include <utility>
#include <cmath>
int main(){
    using namespace std;
    int m, k , T;
    cin >> m;
    int sx, sy
    pair<int,int>sx,sy;
    int tx, ty;
    pair<int,int>tx,ty;
    dx = sx - tx;
    dy = sy - ty;
    distance = sqrt((double) dx*dx + dy*dy);
    while( k > 0 ) {
        cout << k*m << k*m+1 << endl;
        k++
    }
}